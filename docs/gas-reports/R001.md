# Gas Report — SPEC-R001 (AgentPassport)

**Generated:** 2026-05-23 (v3 — post review-fixes: ZeroAgentId guard + relinquish SLOAD cleanup)
**Toolchain:** forge 1.7.1, solc 0.8.24, optimizer on (runs = 200)
**Command:** `forge test --gas-report`

## Deployment

| Metric | v1 | v2 | v3 | Δ (v2→v3) |
|---|---:|---:|---:|---:|
| Deployment Cost | 671,506 | 905,253 | 913,049 | +7,796 (+0.9%) |
| Deployment Size | 2,888 bytes | 3,969 bytes | 4,005 bytes | +36 (+0.9%) |

v3 Δ is the two extra `ZeroAgentId` guards on update/relinquish (one `JUMPI` each).
Still far within contract size limits (24,576).

## Per-Function Gas Cost (v3)

| Function | Min | Avg | Median | Max | # Calls | Notes |
|---|---:|---:|---:|---:|---:|---|
| `registerAgent` | 24,144 | 195,856 | 188,167 | 301,281 | 288 | Min = early revert (input validation) |
| `updateAgent` | 24,442 | 27,357 | 26,982 | 53,191 | 268 | Min dropped (new ZeroAgentId early-revert path) |
| `relinquishAgent` | 21,491 | 31,144 | 31,569 | 39,240 | 6 | Min dropped (ZeroAgentId early-revert); max −107 from dropped SLOAD |
| `agentOwner` | 2,543 | 2,543 | 2,543 | 2,543 | 3 | **~8× cheaper than resolveAgent.owner** |
| `exists` | 2,457 | 2,457 | 2,457 | 2,457 | 3 | **~8× cheaper than resolveAgent.owner** |
| `resolveAgent` (view) | 13,457 | 19,804 | 18,518 | 29,344 | 269 | Full struct decode |

## NFR1 Compliance Check

**Target:** `registerAgent` < **250,000 gas** for strings ≤ 50 chars.

- **In-test assertion** (v2 NEW): `test_RegisterAgent_GasUnder250k` runs in CI; will fail if a future
  refactor exceeds the budget (e.g., R002 vtable dispatch + extra SLOAD).
- **Unit tests** (fixed strings, all ≤ 50 chars): every call ≤ ~190k ✓
- **Fuzz test** (`testFuzz_RegisterArbitraryCards`): bounded strings up to 64/128/128 chars
  → max 301k gas with worst-case multi-byte unicode payloads (out of spec)
- **Median across all suites:** 188k ✓

**Conclusion:** NFR1 satisfied. The earliest-revert path (24k) confirms input validation runs
before the expensive SSTOREs — bad inputs cost almost nothing.

## resolveAgent vs agentOwner / exists

The single biggest v2 efficiency win for downstream consumers (R003, R007, R008):

| Use Case | v1 path | v2 path | Savings |
|---|---|---|---|
| "Who owns this agent?" | `resolveAgent(id).owner` (~20k) | `agentOwner(id)` (~2.5k) | **17.5k per call** |
| "Does this exist?" | `resolveAgent(id).owner != address(0)` (~20k) | `exists(id)` (~2.5k) | **17.5k per call** |

At scale (R003 JobContract authorizing every provider, R007/R008 adapters per trade), this is
real money saved on every call.

## How to Reproduce

```bash
cd contracts
forge test --gas-report
```
