# Gas Report — SPEC-R001 (AgentPassport)

**Generated:** 2026-05-23
**Toolchain:** forge 1.7.1, solc 0.8.24, optimizer on (runs = 200)
**Command:** `forge test --gas-report`

## Deployment

| Metric | Value |
|---|---|
| Deployment Cost | 671,506 gas |
| Deployment Size | 2,888 bytes |

## Per-Function Gas Cost

| Function | Min | Avg | Median | Max | # Calls |
|---|---:|---:|---:|---:|---:|
| `registerAgent` | 26,698 | 200,677 | 187,956 | 301,076 | 271 |
| `updateAgent` | 26,197 | 27,011 | 26,797 | 52,341 | 264 |
| `resolveAgent` (view) | 13,391 | 19,861 | 18,452 | 29,278 | 269 |

## NFR1 Compliance Check

**Target:** `registerAgent` < **250,000 gas** for strings ≤ 50 chars.

- **Unit tests** (fixed strings, all ≤ 50 chars): every call ≤ ~190k ✓ (well under budget)
- **Fuzz test** (`testFuzz_RegisterArbitraryCards`): allows strings up to 64/128/128 chars
  → max 301,076 gas with worst-case multi-byte unicode payloads
- **Median across all suites:** 188k ✓

**Conclusion:** NFR1 satisfied for the contracted input range (≤ 50 char strings).
Callers using larger payloads should expect linear growth; the gas budget is
a per-call statement, not a hard contract invariant.

## resolveAgent Note

`resolveAgent` is a view function — on-chain calls (during writes from other
contracts) incur the 13k-29k cost above, but RPC reads from off-chain are free.

## How to Reproduce

```bash
cd contracts
forge test --gas-report
```
